{{#with comment}}
    {{#if hasVisibleComponent}}
        <section class="tsd-comment tsd-typography">
            {{#if shortText}}
                <div class="lead">
                    {{#markdown}}{{{shortText}}}{{/markdown}}
                </div>
            {{/if}}
            {{#if text}}
                {{#markdown}}{{{text}}}{{/markdown}}
            {{/if}}
            {{#if tags}}
                <dl class="tsd-comment-tags">
                    {{#each tags}}
                        {{#ifCond tagName '!=' 'description'}}
                            <dt>{{tagName}}</dt>
                        {{/ifCond}}
                        <dd>{{#markdown}}{{{text}}}{{/markdown}}</dd>
                    {{/each}}
                </dl>
            {{/if}}
        </section>
    {{/if}}
{{/with}}
